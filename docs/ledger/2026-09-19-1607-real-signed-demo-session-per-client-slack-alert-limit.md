# 2026-09-19 16:07 CDT - Real signed demo session, per-client Slack alert limit
- **Who:** Claude Opus 5 (1M context), sub-agent dispatched for audit findings L1, L4 and W8.
- **Change:** `/api/session` now mints an HS256 session JWT (jti, iat, exp, 1h) instead of the literal `demo-user`; `src/middleware.ts` verifies it with jose in the Edge runtime; `/api/alerts/slack` and `/api/anomalies/[id]/status` verify it instead of checking cookie presence. `/api/alerts/slack` adds a 2-per-minute per-client window keyed on `CF-Connecting-IP` on top of the kept 5-per-minute global ceiling (`src/lib/alert-rate-limit.ts`). Tier map, verify assertions, CLAUDE.md, `.env.example` and `docs/PORTAL_FEDERATION.md` updated.
- **Why:** the session gate was decorative (any cookie value passed) and one anonymous client could exhaust the global alert window. L4 (used-jti replay set for portal handoff tokens) was NOT built: the portal's `mintAccessToken` (portal-shell `src/lib/jwt-signing.ts`, origin/main 96e78ee) sets no `jti`, so there is no stable id to remember; hashing the whole token instead was rejected as out of scope for this finding. Full design in `docs/demos/lumen/decisions.md` (2026-09-19, real demo session).
- **State after:** unmerged PR on `fix/real-demo-session`. The deploy env MUST carry a 32+ char `SESSION_SECRET` before this ships, or demo sign-in fails closed with 500 and the live demo is locked (whether the live container has one today is unverified). Portal handoff replay stays open until the portal mints a jti.
- **Refs:** branch fix/real-demo-session, src/lib/portal-session.ts, src/middleware.ts, src/lib/alert-rate-limit.ts, docs/demos/lumen/decisions.md.
