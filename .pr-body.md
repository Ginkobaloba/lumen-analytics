## Chunk 2.6: anomaly drill-down side panel

The success-criterion interaction: clicking an anomaly (chart marker or feed item) opens the cause-attribution side panel with working drill-down.

- `GET /api/anomalies/[id]`: full detail payload -- anomaly row, expected-vs-actual series (expected = trend + seasonal from the same decomposition the detector ran, with 45 days of context each side), and the same view per top contributing slice.
- `POST /api/anomalies/[id]/status`: Acknowledge / Assign / Mark as False Positive transitions (`GET /api/team` feeds the assign dropdown). Writes are container-layer demo state per the agreed posture.
- Panel UI (shadcn Sheet): severity + status badges, title/summary, expected-vs-actual chart with the episode window shaded in the severity color, "What changed" with top-3 slices (lift, contribution share, mini chart each), suggested next steps, action buttons with optimistic updates + router.refresh.
- Overview is now interactive end to end: markers and feed open the panel (new `OverviewExplorer` client island; `AnomalyFeed` converted to client with onSelect).
- `createDb` resets the database in place when the file is locked by a running server (Windows EBUSY), so `npm run seed:full` works during dev.
- Fixed a Radix a11y violation (Sheet without a title during the loading skeleton).

Verified in the running app: opened the Critical API-adoption anomaly from the feed; panel showed Software -23% / Growth -16% / APAC -8% with mini charts and spec-style suggested actions; Acknowledge flipped Active to Acknowledged and persisted; dataset then restored via `npm run seed:full`. 32/32 tests (2 new suites for detail payload + workflow transitions), tsc, eslint, production build green.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
